POSTGRES_DB=sms_edge
POSTGRES_USER=sms
POSTGRES_PASSWORD=changeme
TZ=UTC

# API
API_PORT=8081
# Optional: overrides DSN; if empty, built from POSTGRES_* vars
DATABASE_URL=postgres://sms:changeme@db:5432/sms_edge?sslmode=disable
DEV_MODE=true
CORS_ALLOW_ORIGIN=*
